# 6fa1a60 — Add changelog entries for ddf89b3 and 21f6ccb, fix numbering

Backfills two more missing entries and corrects a numbering mistake from the
previous round: the discount-tests commit (`21f6ccb`) had been mislabeled `#8`
instead of `#9`, which skipped `ddf89b3` (itself a changelog-only commit) entirely.

## What changed
- `history/8-ddf89b3.md`: new entry, documenting the changelog-only commit that
  added entries for `7899ffc` and `42074cb`.
- `history/9-21f6ccb.md`: the discount-tests entry, moved to its correct number.
- `history/README.md`: table renumbered to match.

No code changes.

(This entry itself was added retroactively, one round later than it should have
been — see `history/11-f3a7e10.md` and `history/README.md`'s table for how the gap
was found and closed.)
