# fa044d5 — Add price-by-region test case (TDD case 3), changelog for previous commit

## What changed
- `apps/backend/integration-tests/http/catalog-channels.spec.ts`: added TDD case 3 from
  `docs/tdd/medusa-catalog.tdd.md` — a cross-listed variant's price must resolve to the
  correct currency/region for each brand, not just be visible under both. The test's
  `beforeAll` now also creates a "Test US Region" (USD) and "Test JP Region" (JPY), and
  a new `it` block hits `GET /store/products/:id?region_id=...` for each region under
  the matching brand's publishable key, asserting `calculated_price.currency_code` and
  `calculated_price.calculated_amount` come back as `usd`/20 and `jpy`/3000
  respectively.
- `docs/specs/19-milestones.md`: updated the Phase 1 integration-test bullet from
  "cases 1-2" to "cases 1-3, 4/4 passing."
- `history/ba67aa8.md`: backfilled changelog entry for the previous commit.

## Verified
- `npx tsc --noEmit` on the backend — clean.
- `npm run test:integration:http --workspace=@dtc/backend` — 4/4 passing against the
  same real local Postgres used for the previous commit's verification.
- `npm run lint --workspace=@dtc/backend` — clean apart from the same expected JPY
  false-positive warning noted in earlier entries.

## Not done / deferred
- TDD cases 4-6 (shared inventory linkage: a real order decrementing shared stock,
  visible from both brands) and 7-8 (concurrent-checkout oversell prevention) are not
  covered yet. They need a real cart → checkout → order-complete flow (cart, shipping
  method, payment session via `pp_system_default`) rather than the direct
  workflow/HTTP-GET calls this test file uses so far — a meaningfully larger unit of
  work, flagged rather than attempted partially.
